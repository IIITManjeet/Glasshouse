{
  "name": "glasshouse-subgraph",
  "version": "0.5.0",
  "private": true,
  "license": "MIT",
  "description": "Subgraph for GlasshouseBook on Base: sealed second-price auctions for the right to fill a SwapVM order.",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy glasshouse-base",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.38.1"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
